<a href="{{ path('app_design_show', {"slug": design.slug, "id": design.id, "_locale": app.locale}) }}" class=" group w-full max-w-[400px] h-[450px] rounded-lg relative overflow-hidden">
    <img class="w-full h-[450px] object-cover rounded-lg z-10 bg transition-transform duration-300 group-hover:scale-125" src="{{asset(design.cover)}}" alt="logo of project: {{design.title}}">
    <div class="z-20 h-[450px] rounded-lg p-4 absolute bottom-0 w-full flex flex-col items-end justify-end gap-2 bg-gradient-to-t from-white/80 dark:from-black/80 from-20% to-transparent to-70%">
        <h3 class="font-bold text-xl">{{design.title}}</h3>
        <span class="font-medium">{{design.type}}</span>
    </div>
</a> 